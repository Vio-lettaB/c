#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int n;
int i;

int prostota(int i)
{
    int k = 2;
	for (; k <    i; k++)
	{
		float p;
        float y;
        float m;
        float r;
        int s;
        float q;

        /*printf(" число i %d", i);*/
       /*printf(" число k %d", k);*/
        s = i/k;
        /*printf(" число s %d", s);*/
        r = s;
        /*printf(" число r %f", r);*/
        y = i;
        /*printf(" число y %f", y);*/
        m = k;
        /*printf(" число m %f", m);*/
        p = y/k;
        /*printf(" число p %f", p);*/
        q = p - r;
        /*printf(" число q %f", q);*/
        if (q > 0.0)
        {
        	return 1;
        }
    return 0;

	}


}

int main()
{
    printf(" введите число N\n" );
    scanf(" %d", &n);
    while (n < 2)
    {
       printf(" N не простое ");
    }
   for(i = 2; i <= n; i++)
   {
        if (prostota( i ) == 1 )
        {
            printf (" %d\n", i );
        }

   }
}
